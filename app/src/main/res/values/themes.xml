<resources>
    <!-- Compose draws everything; this just avoids a flash before setContent. -->
    <style name="Theme.BarCalc" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/app_background</item>
    </style>
</resources>
